# Horizon Essentials Configuration Section
# 플러그인 개발 milkyway0308 / KiwiYou

# 현재 콘피그의 버전입니다.
# 플러그인 교체 후, 이 버전이 최신 버전보다 낮다면 파일을 재생성시켜주세요.
# 파일은 configutation.txt를 폴더에서 삭제하면 재생성됩니다.
콘피그 버전:6
# 이 옵션은 TPAsk(tpa)를 사용 후,수락 가능 시간을 설정합니다. 
텔레포트 요청 시간:180초

# 이 옵션은 TPAsk(tpa) 수락 후 대기 딜레이입니다.
텔레포트 대기 시간:3초

# 이 옵션은 tpa 사용 시,움직이면 텔레포트가 중단될지의 여부입니다.
텔레포트 동작 중단 여부:true

# 색깔 채팅 사용 여부입니다.
# true로 설정시,관리자 권한(OP) 혹은 horizonessentials.colorchat 퍼미션을 가진
# 플래이어의 색깔채팅이 리플레이스됩니다.
색깔채팅:true

# 관리자 채팅 닉네임 색 설정입니다.
# 만약 이곳에 문자를 적으신다면,색깔 대신 관리자 칭호로 사용도 가능합니다.
# 닉네임이 설정되었을 경우 이 옵션은 해당 플레이어에게 효과가 없습니다.
관리자 채팅:&4

# PermissionsEX으 칭호 사용 여부입니다.
# PermissionsEX가 없으면 작동하지 않는 옵션입니다.
퍼미션 칭호:false

# 이코노미 메인 통화입니다.
이코노미 기본 통화:골드

# 이코노미 서브 통화입니다.
이코노미 서브 통화:실버

# 서브 통화를 표시할지의 여부입니다.
# true로 할 시,돈 조회시 100.0의 잔액이 남아있고,메인 통화가 골드,서브 통화가 실버일경우 100골드 0실버로 표시됩니다.
# false로 할시,돈 조회시 100.0의 잔액이 남아있고,메인 통화가 골드라면 100.0 골드로 표시됩니다.
이코노미 서브 통화 사용:false

# 이코노미 서브 통화 사용을 하지 않을때에만 작동합니다.
# true로 할시,통화의 소숫점까지 표시합니다.(100.0)
# false로 할 시,통화의 소숫점을 표시하지 않습니다.(100)
이코노미 소숫점 표시:false

# 플레이어 최초 접속 / 최초 계좌 생성시에 기본으로 주어질 돈입니다.
이코노미 기본:1000

# 최소로 줄어들 수 있는 플레이어의 돈입니다.
이코노미 최소:-5000

# 최대로 얻을수 있는 플레이어의 돈입니다.
이코노미 최대:999999999999999999

# 이벤트 캔슬 저장 여부입니다.
# true로 설정시 일정 시간마다 현재 상태(State)가 자동으로 저장되며,
# 서버가 리부팅되거나 플러그인 리부팅시에 데이터를 불러옵니다.
# false로 할시,이벤트 캔슬 여부가 자동으로 false로 되돌아갑니다.
이벤트 데이터 저장 여부:false

# 최초 접속시 sethome 허용 개수입니다.
# 1일시,default 홈만 설정이 가능합니다.(/sethome)
# 2 이상일시,default 홈 제외의 홈 설정이 가능합니다.(/sethome [홈이름])
기본 홈:1

# 감옥 탈출 시도(텔레포트)시,추가될 시간의 양입니다.
# 0 이하로 할시,추가되지 않으며 0으로 설정됩니다.
감옥 탈출 패널티:30

# /summon 명령어로 한번에 소환 가능한 엔티티의 개체수입니다.
엔티티 소환 제한:35